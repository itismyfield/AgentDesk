# AgentDesk Agent Guide

For canonical edit paths across runtime config, prompts, policies, memory,
LaunchAgent files, database routing, `CLAUDE.md` variants, and MCP mirrors, see
[`docs/source-of-truth.md`](docs/source-of-truth.md).

Treat duplicated config references in older docs as deprecated unless they point
back to the source-of-truth index.
