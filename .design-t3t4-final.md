# #5071 T3·T4 통합 설계 FINAL — 4987 결속 최소안

**측정 SHA:** `9964338e8a3060ceddef26c76ab4acf851dc2cb0` (`main`). 모든 저장소 행번호와 수치는 이 SHA 기준이다. 설계 작성 중 저장소 코드 수정·빌드·테스트는 하지 않았다. `git status --short`는 시작 시 공집합이었다.

이 라운드의 결론은 **축소**다.

- T4는 `docs/design/4987-relay-reachability.md`의 R2 개정판을 그대로 구현한다. 새 판정기, 새 파괴 권한, 새 복구 액터를 덧붙이지 않는다.
- T3는 4987의 `IncarnationRange`가 요구하는 기존 identity 입력을 읽을 수 있게 하고, 이미 존재하는 watcher-registry CAS로 닫을 수 있는 세션명 소비처만 fence한다.
- 이전 라운드의 `cancel_tombstone` 재키잉, 재도출 단일액터, `DestructionActor`, `DestructionWarrant`, adoption stamping, `authority.lock`, 공통 destroyer lease는 전부 폐기한다.
- 이 최소안이 닫지 못하는 cross-node, process backend, ownerless, marker read→act 창은 새 기계로 덮지 않는다. §8의 비보장과 T5 이관으로 남긴다.

4987의 문서 내부 기준 SHA는 `dd46cc807`이지만, 이 문서의 코드 좌표는 위 측정 SHA다. 4987에서 R2가 대체한다고 명시한 절은 반드시 `§-1`을 우선한다.

---

## 1. R1 P1 분류표

분류는 다음 셋뿐이다.

1. **① 4987 결속으로 자연 소멸** — 이전 설계가 만든 기계를 없애면 공격 표면도 사라진다.
2. **② 최소 수정으로 폐쇄** — 기존 타입·CAS·판정 funnel을 좁게 보강한다.
3. **③ 비보장 선언 + T5 이관** — 이번 시리즈가 보장하지 않는다. 각 행에 실제 사고 시나리오를 적는다.

| R1 리뷰 P1 | 주분류 | R2 처리 | ③이 열어두는 실사고 시나리오 |
|---|---|---|---|
| P1-1: bounded async PG journal은 영구 미기록될 수 있어 cross-node 정온 증명이 아니다 | **③** | PG journal을 T3 identity CAS의 근거나 T4 reachability의 canonical authority로 쓰지 않는다. 4987의 host-local 판정 범위를 유지한다. T5에서 동기 admission 또는 cluster authority를 별도 설계한다. | 노드 A의 `try_send`가 drop되어 emission이 PG에 영구 미기록되고, 노드 B가 “조용함”으로 오판해 같은 채널의 정리/복구를 실행할 수 있다. |
| P1-2: `DestructionActor` 전수가 process kill과 registry-only 제거를 누락 | **①** | 액터와 “모든 파괴가 액터 경유” 보장을 함께 폐기하므로 actor 전수 반례가 소멸한다. 지도 누락은 §2에서 최소 정정한다. process kill/직접 remove의 새 전역 보장은 만들지 않으며 그 잔여는 §8에 별도 ③으로 선언한다. | 해당 없음(잘못된 전수 보장을 제거). 잔여 사고는 §8 process/registry 행. |
| P1-3: 기존 plan slot과 `DeliveryLeaseCell`의 중첩 획득 | **①** | emission cell 이관과 `LeaseHolder::Destroyer`를 하지 않는다. 따라서 non-reentrant 이중 획득 경로가 생기지 않는다. | 해당 없음. 단, 공통 lease를 도입하지 않으므로 local/cross-node emission 배타 보장은 별도 ③으로 남는다. |
| P1-4: `DestructionWarrant` ownerless 증명이 증거 부재를 부재 증거로 오독하며 capability도 미봉인 | **①** | warrant와 ownerless warrant 발급 규칙을 삭제한다. T3 최소 경로의 positive owner/handle/nonce 검사는 새 warrant가 아니라 기존 registry CAS 보강이다. ownerless 자동 파괴 전역은 §8에 별도 ③으로 남긴다. | 해당 없음(불건전 warrant를 발급하지 않음). 잔여 ownerless 사고는 §8 행. |
| P1-5: A3/A4 순서 모순, G1/G4/G5가 기계 게이트가 아님 | **①** | adoption stamping, `authority.lock`, G1/G4/G5를 전부 없앤다. 이 R2의 게이트는 명령·카운터·고정 임계값으로 판정되는 것만 §4.4에 둔다. | 해당 없음. marker 부재는 안전성 증명으로 쓰지 않고 `Unknown/Deny` 또는 비보장으로 남긴다. |
| P1-6: 4987의 B1′/GO 순서와 충돌하고 A∥B disjoint도 거짓 | **②** | 4987 S1은 두 PR로 물리 분할하되 첫 PR은 비활성 내부 라이브러리다. S1 관측 루프를 활성화하는 두 번째 PR에 Rust↔Python fixture+mutation을 함께 넣는다. S6은 4987 identity/read-complete 계약 이후다. registry를 함께 만지는 T3-A1과 T4-B0는 직렬화한다. | 해당 없음. |
| P1-7: D1 삭제 이중계상, 예산 과소·과대 주장 | **②** | §6에서 4987 §9.2의 숫자를 그대로 다시 더한다. T3는 구현 diff 전 순증감을 가격하지 않고, 삭제 후보를 크레딧하지 않는다. “정본 미계상” 귀책 주장은 쓰지 않는다. | 해당 없음. |

**승계 재발 판정:** r1 P1의 공통 lease/requester/cohort/actor 문제가 다른 이름으로 재발하지 않도록 그 네 기계를 설계에서 제거했다. 남는 약한 보장은 §8에 이름과 사고를 붙여 공개한다.

---

## 2. 전수 지도 최종본

### 2.1 방법론과 한계

측정은 다음 슈퍼셋을 직접 실행하고 좌표를 수동 판독했다.

```bash
# tmux kill wrapper
rg -n '\bkill_session(_output(_timeout)?|_checked)?\s*\(' src --glob '*.rs'

# watcher cancel: 줄바꿈을 반드시 포함
rg -U -n --multiline-dotall 'cancel\s*\.\s*store\s*\(\s*true' src --glob '*.rs'
rg -n 'cancel_for_commit\s*\.\s*store|expected_cancel\s*\.\s*store|watcher_cancel\s*\.\s*store' src --glob '*.rs'

# process backend 파괴 — tmux wrapper와 별도 축
rg -n '\b(kill_pid_tree|terminate_process_handle)\s*\(' src/services/discord --glob '*.rs'
rg -n '\.kill\(\)' src/services/discord --glob '*.rs'

# registry 제거 — 직접 remove와 CAS helper를 모두 포함
rg -n 'tmux_watchers\s*\.\s*remove|remove_tmux_session_(if_current|locked)|cancel_and_remove_channel_if_current' \
  src/services/discord --glob '*.rs'

# spawn/marker
rg -n 'platform::tmux::create_session\s*\(' src --glob '*.rs'
rg -n 'stamp_spawn_markers\s*\(' src --glob '*.rs'
```

정규화 규칙:

1. multiline 결과는 매치 시작행 1건으로 센다.
2. 정의, 테스트, 주석을 수동으로 구분한다. 이 저장소는 외부 `mod tests`, 파일 중간 `#[cfg(test)]`, `#[cfg(all(test, unix))]`가 있어 “첫 cfg 이후는 test” 자동분류가 틀린다.
3. `tmux_session_name`은 locator까지 포함해 **4,505 occurrences / 274 files**다. 이 수치는 변경 정의역이 아니다. 의미별 축으로 분류한 뒤 파괴/권위 소비자만 fence 후보로 삼는다.
4. 정규식은 지도와 ratchet의 입력이지 전수 타입 증명이 아니다. process kill처럼 다른 함수군은 별도 패턴이 필요하다.
5. `.claude/worktrees/**`, `/private/tmp/**`, 배포/CI가 dcserver 자체를 재기동하는 tmux 명령은 런타임 identity fence 정의역에서 제외했다.

### 2.2 세션명 소비의 다섯 축

| 축 | 의미 | R2 처리 |
|---|---|---|
| A. 유도 | `build_tmux_session_name` 등 이름 생성 | 변경 안 함. 이름은 계속 locator다. 유일화는 T5. |
| B. 권위키 | live registry, restored owner, dedupe mirror, `.channel`, settings binding | 4987 S0의 transcript discovery 입력으로 live registry를 읽는다. 나머지를 새 canonical registry로 승격하지 않는다. |
| C. 파괴대상 | tmux kill, watcher cancel, process kill, registry remove | 아래 전수표. T3는 음영 없는 최소 CAS 경로만 변경한다. |
| D. identity conjunct | `.generation` mtime, `.spawn_nonce`, watcher handle pointer, output path | 4987에 필요한 최소 입력. 새 `execution_id` 저장소를 만들지 않는다. |
| E. locator | transcript/marker/pane/session lookup | 대량 소비자 274파일을 이관하지 않는다. locator 사용은 허용한다. |

### 2.3 파괴·생성 표면

| 표면 | 실측 결과 | 주요 좌표/정규화 | R2 운명 |
|---|---:|---|---|
| tmux kill wrapper | 슈퍼셋 **51/26파일**; 정의 4 + `platform/tmux.rs` 내부 test 3 제외 후 외부 **44사이트/25파일** = prod **37/20**, test **7/5** | `services/platform/tmux.rs:370-392`가 네 wrapper 정의 | 전역 proof-required 시그니처 이관은 하지 않는다. 기존 positive identity가 있는 자동 경로만 T3-A1/A2, 나머지는 T5 비보장. |
| watcher cancel | raw 23 + alias 2 − 주석 FP 2 = 실사이트 **23**; prod **14/9파일**, test **9/8파일** | prod: `health/recovery.rs:936,1030`, `inflight_heartbeat_sweeper.rs:230`, `relay_recovery/apply.rs:130,251`, `runtime_bootstrap/gateway_lease.rs:437`, `tmux_watcher_registry.rs:250`, `tui_direct_pending_start.rs:883`, `runtime_handoff_loop.rs:460`, `watcher_handoff.rs:41,386`, `claims.rs:211,376,427` | `apply.rs:251`과 `tui_direct_pending_start.rs:883`의 store-before-registry-CAS 형제만 같은 최소 CAS 슬라이스에서 닫는다. 나머지는 ratchet에 고정하고 T5. |
| process backend kill | prod **5사이트/4파일** | `commands/control.rs:124,128`; `turn_bridge/tmux_runtime.rs:646`; `.../process_backend_cancel.rs:155`; `tmux_reaper.rs:775`. `tmux_runtime.rs:924,973`와 `liveness_authority.rs:732`는 test | tmux nonce로 PID identity를 증명할 수 없다. 이번 T3 fence 밖, T5. |
| registry mutation | 외부 prod **17사이트/11파일** | 아래 §2.4 | stale callback에 이미 handle이 있는 두 종료 경로와 H6 형제만 최소 수정. 나머지는 전역 수렴을 보장하지 않는다. |
| tmux create | qualified 외부 호출 **13** = prod **6**, test **7** | prod `claude.rs:2011,2811`, `codex.rs:1875,2392`, `qwen.rs:1364`, `cli/dcserver.rs:986` | marker writer는 prod 5개 provider spawn에 있고 CLI `dcserver.rs:986`에는 없다. 부재를 위조하지 않고 `spawn_nonce=None`으로 유지. |
| marker stamp | prod 호출 **5** | `claude.rs:1807,2836`, `codex.rs:1898,2413`, `qwen.rs:1390`; 정의 `tmux_session_files.rs:141` | 신규/backfill writer를 만들지 않는다. |

cancel prod 14곳의 의미 분류는 이전 r2 직접 판독과 현 SHA 재검색이 일치한다. test 9곳은 `turn_finalizer.rs:4137,4634`, `manual_rebind/mod.rs:1295`, `relay_auto_heal.rs:2238`, `placeholder_reclaim.rs:344`, `relay_recovery_auto_heal_confirm.rs:255`, `tmux_watcher_registry_restore_tests.rs:203`, `watchers/lifecycle/tests.rs:236`, `inflight/destructive_commit.rs:312`다. FP는 `health/watcher_respawn.rs:9`와 `runtime_handoff_loop.rs:333`이다.

### 2.4 registry 제거 17곳 — Drop/registry-only 보완

| 형태 | prod 좌표 | 수 | 최소 처리 |
|---|---|---:|---|
| 채널 키 직접 remove (`tmux_watchers.remove` 또는 `remove_locked`) | `relay_recovery/apply.rs:129`; `tmux_watcher/post_stream_exit.rs:33`; `turn_finalizer/watcher_backstop.rs:252`; `health/recovery.rs:935,1027`; `runtime_handoff_loop.rs:341,458`; `watcher_handoff.rs:40,244,383` | **10** | `post_stream_exit.rs`는 이미 `cancel`과 `tmux_session_name`을 보유하므로 기존 `remove_tmux_session_if_current`로 교체한다. `apply.rs:129`는 idle_tmux_repair 분기의 채널 키 remove이며 H6이 아니다 — A1이 닫는 H6 사이트는 §2.3의 `apply.rs:251`(store)·`:270`(cancel_and_remove)이고 이 행의 `:129`는 T5 ratchet 고정이다. **[A1-r1 legC P2-5 정오]** `health/recovery.rs:1027`은 `remove_locked(&guard, &channel_id)`이며 `:1013-1026`에서 `channel_binding` 기반 세션명을 재확인한다. 나머지는 T5 ratchet 고정. **[r2-리뷰 P2-1 반영]** |
| `remove_tmux_session_if_current` | `task_supervisor.rs:63` (**`Drop`**), `tui_direct_pending_start.rs:908` | **2** | Drop 경로는 이미 `Arc::ptr_eq(cancel)` CAS를 사용한다. T3는 회귀 테스트만 추가한다. TUI 형제는 A1에서 nonce 관측을 추가한다. |
| `cancel_and_remove_channel_if_current` | `relay_recovery/apply.rs:270`, `manual_rebind/mod.rs:825`, `health/relay_auto_heal.rs:724` | **3** | 기존 registry lock 안의 `(channel, session, output_path, cancel pointer)` CAS를 보존. A1은 apply 경로에 nonce 재확인을 추가한다. |
| `remove_tmux_session_locked` 외부 호출 | `watchers/lifecycle/claims.rs:208,373` | **2** | 같은 registry mutex를 쓰지만 nonce fence는 없다. T5. |

두 P1 보완점의 결론:

- `task_supervisor.rs:58-67`의 `Drop`은 **누락됐지만 이미 current-handle CAS를 통과한다**. 새 액터가 필요하지 않다.
- `post_stream_exit.rs:28-34`는 cancel되지 않았다는 불리언만 보고 직접 channel remove한다. 늦은 old watcher가 새 slot을 지울 수 있으므로 T3-A2에서 기존 CAS helper로 바꾼다.

### 2.5 권위/identity 입력 지도

| 입력 | 현재 권위와 좌표 | 4987에 공급하는 값 | R2 변경 |
|---|---|---|---|
| provider | 채널 런타임 설정 | `IncarnationRange.provider` | 없음 |
| tmux session | live watcher registry 또는 mailbox cancel token; 4987 §-1.2/§-1.3 | `tmux_session_name` | 없음 |
| generation | `tmux_session_files.rs:20` `read_generation_file_mtime_ns` | `generation_mtime_ns` | 없음 |
| spawn nonce | `tmux_session_files.rs:172` write, `:203` read | `spawn_nonce: Option<String>` | T3-A0이 얇은 read model로 노출. `None`을 wildcard로 만들지 않음 |
| transcript path | handle에는 `tmux_watcher_registry.rs:9-12`로 있으나 binding(`:91-94`)은 아직 미노출 | discovery 우선순위 1 | 4987 S0 그대로 T4-B0에서 `TmuxWatcherBinding.output_path` 추가 |
| file identity/range | T4 discovery/parser가 stat/tail | `(dev,ino)`, byte range | 4987 S1 그대로 |
| owner channel | live registry primary + restored owner fallback `tmux_watcher_registry.rs:272-289` | **IncarnationRange 항이 아님**. T3 local auto-cancel 대상 검증에만 사용 | `.channel`을 새 canonical authority로 승격하지 않음 |

권위 5개 수치는 승계한다: live `owner_channel_by_tmux_session`, restored owner(prod 소비 10), TUI dedupe mirror(prod 소비 6), `.channel`(현 prod read 1, 제한적 writer), settings-derived binding. R2는 이를 삭제하거나 하나의 새 레지스트리로 합치지 않는다.

---

## 3. 불변식과 경계 — 새 발명 최소화

### 3.1 4987 계약을 그대로 인용하는 T4 불변식

이 문서는 다음을 재정의하지 않는다.

1. **판정 좌표:** 4987 §-1.3 `IncarnationRange = (provider, tmux_session_name, generation_mtime_ns, spawn_nonce?, transcript_file_id, byte range)`. `turn_nonce`, `user_msg_id`, inflight row는 의무 좌표가 아니다.
2. **증거 사다리:** 4987 §2.3의 E0 `Obligated`, E1 `ConfirmedDeliveryReceipt`, E2 `DeliveredCommit`; E3는 감사이며 배달 정의가 아니다.
3. **극성:** 4987 §4.1. `ReachabilityVerdict != Reachable`이면 구조 신호가 GREEN이어도 최종 건강은 GREEN이 아니다.
4. **외부 판정:** 4987 §-1.5/§5. `read_complete=false` 또는 incarnation mismatch는 `Unknown`/무시이며, external verdict는 같은 incarnation에서 악화만 가능하다.
5. **typed 소멸:** 4987 §-1.5의 `ReceiptCovered | ClassifiedDrop{reason} | IncarnationRetired`; 암묵적 frontier/grace 소멸 금지.
6. **행 독립:** 4987 §-1.5 I14의 소스 린트. 이는 타입 증명이 아니라 lint임을 그대로 명시한다. 집행기는 **T4-B1**이며, 전용 게이트에서 `scripts/check_reachability_row_independence.py`를 `ci-script-checks.sh`에 배선하고 rc=0을 요구한다. 같은 B1 문서 게이트가 `docs/agent-maintenance/change-surfaces.md`의 신규 `relay_reachability` surface도 집행한다. **[r2-리뷰 P1-4 반영]**
7. **비파괴:** 4987 §7.1/I15. `ReachabilityVerdict` 단독으로 turn cancel, tmux/process kill, registry remove, mailbox/inflight force-clean을 승인하지 않는다. R2는 warrant/private-constructor 계층을 추가하지 않으며, 이 항의 타입 강제는 보장하지 않는다.

### 3.2 T3가 공급하는 최소 규칙

새 전역 아키텍처가 아니라 기존 CAS의 입력 규칙이다.

- **T3-R1 — read model:** `SessionIncarnationRef = { tmux_session_name, spawn_nonce: Option<String> }`. 저장소는 기존 `.spawn_nonce` 하나다. 새 durable row/marker/PG table은 없다.
- **T3-R2 — positive local ownership:** T3 Enforce 대상 자동 cancel/remove는 기존 live registry에서 `(owner_channel, tmux_session_name, output_path, Arc::ptr_eq(cancel))`가 일치하고, capture한 `Some(nonce)`와 action 직전 `read_spawn_nonce`가 같을 때만 실행한다. `None`, mismatch, ownerless는 그 **최소 경로에서는** Deny다.
- **T3-R3 — H6 순서와 helper 비대칭:** 두 형제의 callback에서는 cancel flag를 쓰지 않는다. `relay_recovery/apply.rs:251→270`의 `cancel_and_remove_channel_if_current`는 `tmux_watcher_registry.rs:249-251`에서 current registry CAS 성공 뒤 내부적으로 cancel을 쓴다. 반면 `tui_direct_pending_start.rs:883→908`의 `remove_tmux_session_if_current`는 `tmux_watcher_registry.rs:202-216`에서 제거만 하고 cancel을 쓰지 않으므로, 이 형제는 helper가 `Some`을 반환한 성공 분기 뒤 명시적으로 cancel을 쓴다. 어느 형제든 registry CAS 실패면 cancel은 false로 남고, TUI watcher가 registry에서만 사라진 채 계속 relay하는 상태를 만들지 않는다. 새 lock/actor가 아니다. **[r2-리뷰 P1-1 반영]**
- **T3-R4 — operator 예외:** 명시적 operator/CLI process reset과 T3 비이관 경로는 이 규칙의 보장 범위가 아니다. 이를 `OperatorDirected` capability로 포장하지 않는다.
- **T3-R5 — fence 동시성:** `health/relay_dead_reattach.rs:88`의 선행 read와 `relay_recovery/apply.rs:216-224`의 후행 read는 같은 호출사슬에 있고 둘 다 `f5850fd99903f04659e42722798bd6fb97caa2de`(#5067)에서 들어왔다. 두 fence 사이트를 T3-A1의 R2/R3 착지와 같은 PR에서 제거한다. **결정 근거:** 측정 SHA `9964338e8a3060ceddef26c76ab4acf851dc2cb0`의 `rg`/`git log -S` 실측에서 두 사이트의 실재·동일 기원·호출사슬을 확인했으므로 A1 편입이 타당하다. 다만 nonce/registry CAS는 emission lease가 아니므로 “상위호환” 또는 #5037 완전 폐쇄라고 쓰지 않는다. **[r2-리뷰 P1-2 반영]**

### 3.3 기존 복구 funnel과의 관계

T4는 새 “복구 단일액터”를 만들지 않는다. 현재의

```
plan_relay_recovery (relay_recovery/decision.rs:314)
  → apply_relay_recovery_plan
  → apply_relay_recovery_decision (relay_recovery/apply.rs:3)
```

funnel에 4987 §4.4대로 `ReachabilityVerdict` 입력을 추가한다. `RelayStallState`는 행동 선택기로 남는다. reachability가 새 destructive branch를 만드는 것은 금지한다. 이 funnel 밖의 kill/cancel/remove를 전부 이관했다는 주장은 하지 않는다.

---

## 4. 구현 슬라이스

### 4.1 공통 기계 게이트

모든 슬라이스는 다음을 PR required artifact로 남긴다.

1. `git diff --numstat <base>...HEAD`를 기계 집계해 **변경 파일 ≤20, additions ≤800, deletions ≤800**. 넘으면 PR을 분할한다. **[A1-r2 정오, 2026-08-16]** 단, 초과가 이 표의 게이트 열이 의무화한 테스트 추가(`#[cfg(test)]` 블록 내 additions, brace-balance 실측) 때문일 때는 **비테스트 additions ≤800**을 적용하되, 기계 총계와 prod/test/산출물 분해를 PR 본문에 명시하고 리뷰가 그 분해를 독립 재측정한다. 분할 불가 사유 없이 이 조항을 원용하는 것은 금지 — 기본은 여전히 분할이다. (첫 적용: A1-r2, 총 +1159 = prod 687 + test 463 + 재핀 9)
2. `cargo fmt --all --check`와 `cargo check --workspace --all-targets` rc=0.
3. 해당 named test lane의 `cargo test ... -- --list` 선택 수를 기록하고 **0이면 실패**.
4. 신규 production branch를 지우거나 극성을 뒤집는 고정 mutation이 최소 1개 죽어야 한다. 컴파일 실패만으로 죽은 mutation은 behavioral 증거로 세지 않는다.
5. inventory/doc generator의 tracked output diff가 의도와 일치해야 한다.

이 설계 세션에서는 위 명령을 실행하지 않았다. 구현 PR의 게이트 계약이다.

### 4.2 T3 — 최소 identity/fence

파일 상한은 **소스 파일 기준**이다. 슬라이스가 파괴 call-site 수를 바꾸면 A4 ratchet 게이트가 `scripts/destructive_call_site_baseline.json`·`scripts/lib_test_inventory_manifest.txt` 재핀을 강제하므로, 이 기계 재핀 산출물 2종은 슬라이스 상한에 세지 않는다 — 세면 상한이 이 표의 게이트 열 자신과 자기모순이 된다. A2 r1 판정(5파일 vs ≤4, 재핀 산출물 분해 후 소스 기준 준수) 선례의 일반화. `#[cfg(test)] mod`로 마운트되는 테스트 전용 파일(`tests.rs`, `*_tests.rs` 류)도 게이트 열이 의무화한 테스트 표면이므로 슬라이스 소스 상한에 세지 않는다 — 상한의 대상은 비테스트 소스 파일이다. **[A1-r1 legA P1-2 정오, 2026-08-16; A1-r3 legF P2-2 보강]**

| 슬라이스 | 내용과 파일 상한 | 단독 착지 동작 | 전용 기계 게이트 |
|---|---|---|---|
| **T3-A0** | 기존 `.spawn_nonce`를 읽는 `SessionIncarnationRef`, `ExecutionIdentityMode={Legacy,Observe,Enforce}`, 관측 카운터. 신규 얇은 모듈 + `tmux_session_files.rs`/config/tests, **≤7파일** | Legacy/Observe 기본. 파괴 동작 변화 없음 | `None`을 일치로 바꾸는 mutation과 nonce mismatch 비교 제거 mutation이 named test에서 사망 |
| **T3-A1** | H6 형제 두 곳의 store-before-CAS 제거와 helper별 store-after-CAS 적용: `apply.rs`는 canceling helper의 내부 store, `tui_direct_pending_start.rs`는 non-canceling helper 성공 뒤 명시 store. 기존 registry CAS에 capture/current nonce 비교. #5067 fence 두 사이트 `health/relay_dead_reattach.rs:88`와 `apply.rs:216-224` **동시 제거**. `apply.rs`, `health/relay_dead_reattach.rs`, `tui_direct_pending_start.rs`, `inflight/destructive_commit.rs`, `tmux_watcher_registry.rs`, config/tests, **≤9파일**. **[r2-리뷰 P1-1 반영] [r2-리뷰 P1-2 반영]** | Legacy는 기존 pointer/output CAS를 유지하되 nonce는 관측만. Enforce는 positive owner+nonce 일치 시만 자동 cancel. 두 임시 fence는 이 PR부터 없음. 두 helper의 cancel 비대칭을 보존하여 성공 시 둘 다 cancel되고 CAS 실패 시 둘 다 cancel=false다. | (a) post-gate nonce swap 시 cancel=false, (b) registry replacement 시 cancel=false, (c) 두 fence 사이트 중 하나를 남기거나 CAS를 우회하는 mutation 사망, (d) 두 callsite 모두 호출된 test-id 증거, (e) apply helper와 TUI helper 각각 CAS 성공 시 cancel=true·실패 시 cancel=false 증거 |
| **T3-A2** | 종료 cleanup 최소 fence: `post_stream_exit.rs:33` 직접 remove를 `remove_tmux_session_if_current(session,cancel)`로 교체; `task_supervisor.rs` Drop 현행 CAS 회귀 테스트. 이는 반환 직후 Drop guard가 수행하는 **동일 helper 호출을 post-stream exit에도 한 번 더 두는 중복**이며 신규 기계가 아니다. **≤4파일** **[r2-리뷰 P2-4 반영]** | 동작은 stale cleanup에 대해서만 엄격 강화. switch와 무관하게 항상 적용 | old watcher 종료 뒤 new handle 삽입 시 old cleanup이 new entry를 보존; helper를 direct remove로 되돌리는 mutation 사망 |
| **T3-A3** | #5021 회계: `reuse_existing_live_watcher`를 applied allowlist에서 제거하고 no-transition/refund를 테스트. `relay_recovery.rs`, auto-heal apply/confirm/tests, **≤5파일** | 파괴/identity와 무관한 회계 정정. 항상 활성 | allowlist에 문자열을 재삽입하면 named test 사망; apply count는 증가하지 않고 refund count가 1 증가 |
| **T3-A4** | 전수 ratchet: tmux kill, watcher cancel, process kill, registry remove의 현재 per-file exact count를 별도 카테고리로 고정. registry baseline 주석은 §2.4의 정정 분류 **10/2/3/2**(총 17)를 사용하고, `health/recovery.rs:1027`을 `remove_tmux_session_locked`로 세지 않는다. scripts/tests, **≤4파일** **[r2-리뷰 P2-1 반영]** | 런타임 변화 없음. 새 우회 증식을 차단 | 네 카테고리 각각 신규 가짜 callsite를 넣는 4 mutation이 scanner를 red로 만듦 |

**순서:** `A0 → A1`; A2/A3/A4는 A0 뒤 병렬 가능하다. `A1`과 아래 `T4-B0`는 모두 `tmux_watcher_registry.rs`를 만지므로 병렬 작업하지 않는다.

T3가 완료돼도 tmux kill 37 prod sites, process kill 5 sites, registry 제거 17곳 전부가 nonce-CAS로 수렴했다고 선언하지 않는다. T3 완료 정의는 위 네 runtime 변경과 ratchet 착지다.

### 4.3 T4 — 4987 슬라이스의 물리 분할

논리 내용은 4987 S0~S6 그대로다. ±800 상한 때문에 S1만 물리적으로 둘로 나눈다.

| 슬라이스 | 4987 대응 | 내용/예상 파일 | 단독 착지 동작 | 전용 기계 게이트 |
|---|---|---|---|---|
| **T4-B0** | R2 S0 | `TmuxWatcherBinding.output_path` 노출 + health 배선, **≤4파일** | 가산 read-only | binding이 output path를 누락하는 mutation 사망. 예상 표적은 `tmux_watcher_registry.rs`(392줄)와 `health/session_enrichment.rs`(296줄)이므로 **giant admission note 대상 없음**. 구현이 `health/snapshot.rs`(1,279줄)를 건드릴 때만 그 파일의 admission note를 요구한다. **[r2-리뷰 P2-2 반영]** |
| **T4-B1** | S1 전반 | `health/reachability` verdict 타입 집합 `Reachable | Degraded | TransportUnknown | Unreachable | Unknown`, discovery·증분 tail reader. `TransportUnknown`은 건강도 재배달 권한도 아니다. `scripts/check_reachability_row_independence.py` + `ci-script-checks.sh`, `docs/agent-maintenance/change-surfaces.md`의 `relay_reachability` surface를 함께 소유한다. **관측 task에는 아직 연결하지 않는 내부 라이브러리**, ≤8파일 **[r2-리뷰 P1-3 반영] [r2-리뷰 P1-4 반영]** | production 판정 변화 없음; “S1 활성”로 세지 않음 | unresolved→`Unknown`, inode divergence, 1MB cap의 순수 테스트; row-independence lint rc=0 및 `inflight::` import mutation 사망; inventory/doc generator tracked output 일치 |
| **T4-B2** | S1 후반 + B1′ | obligation/ledger/verdict, 관측 task 연결, Rust↔Python canonical `(generation,start,end,identity,reason)` fixture, CRLF/partial/multibyte/rotation, mutation runner. ≤12파일 | 여기서 비로소 S1 관측 시작. 판정 권한 없음 | Rust/Python 출력 byte-equal; 어느 한 구현만 바꾸는 mutation 사망; selected test count >0 |
| **T4-B3** | S2 | 신규 `outbound/receipt_index.rs`, `delivery_record.rs +0`, ≤4파일 | read-only | wrong generation receipt가 cover하지 못하고 malformed store가 `Unknown` |
| **T4-B4** | S4 | `divergence.rs` + snapshot 비교 배선, ≤4파일 | 설명/비GREEN 신호만 추가 | same-size/different-inode fixture가 divergence; row path는 obligation producer dependency가 아님 |
| **T4-B5** | S6 | watchdog sidecar identity + `read_complete`, `external_verdict.rs`, ≤5파일 | `RelayVerdictSource=Structural`에서는 shadow | incomplete/stale/wrong-incarnation external verdict가 무시되고 same-incarnation complete verdict만 악화 |
| **T4-B6** | S3 | 판정 합성/권한: `relay_health.rs`, `health/snapshot.rs`, `relay_recovery/decision.rs`, health API, 계약 refs, ≤10파일. B1에서 정의한 `TransportUnknown`을 S3 합성에 포함한다. **[r2-리뷰 P1-3 반영]** | `Composite`에서만 건강 극성 반전. Reachability는 destructive action을 새로 승인하지 않음 | 아래 4987 **§-1.4 「오탐 반례 — 인수 테스트에 필수 포함」 표의 7개 반례** 각각이 정확한 기대 판정을 냄 + `Unknown→Healthy` mutation + reachability-only destructive branch source mutation 사망. §4.4 게이트 필드 7개를 반례로 세지 않는다. **[r2-리뷰 P1-3 반영]** |

**B6 인수 반례의 정확한 앵커와 집합:** 4987 §-1.4 「오탐 반례 — 인수 테스트에 필수 포함」의 다음 7행이다. `Unreachable`이 기대값인 2번 정탐을 제외하고, 표가 금지한 `Unreachable`을 내면 실패다. **[r2-리뷰 P1-3 반영]**

| # | 반례 | 기대 판정 |
|---:|---|---|
| 1 | POST 성공 + receipt write 실패 (크래시 창) | `TransportUnknown`, not `Unreachable` |
| 2 | 다른 generation의 receipt만 존재 | `Unreachable` (정탐 — generation 게이팅 작동) |
| 3 | 이전 턴 블록만 존재, 현재 턴 산문 없음 | `Reachable` (의무 0) |
| 4 | wrapper·native가 같은 크기 다른 inode | divergence 검출 |
| 5 | bounded read가 오래된 메시지 미반환 | 워치독 `Unknown`, dcserver 판정 불변 |
| 6 | placeholder 존재 + terminal receipt 아직 없음 | grace 내 `Reachable`, 초과 시 `Degraded`; `Unreachable` 금지 |
| 7 | ledger malformed | `Unknown{ReceiptStoreUnreadable}`, not `Unreachable` |

**순서:** `B0 → B1 → B2`; B3는 B1과 병렬 가능하나 B6 전에 필요. `B4`는 B1 뒤, `B5`는 B2 뒤. `B6`는 `B2+B3+B4+B5+G-T4` 뒤다.

4987 S5(Tier C 재확인 프로브)는 필수 검출 경로가 아니고 4987 자체가 S3 후 병렬 항으로 둔다. 이 R2에서는 **T5로 이관**한다. S7 재배달은 4987의 NO-GO를 그대로 유지한다.

### 4.4 T4 권한 게이트 `G-T4`

근거 없는 `≈0`, “cohort 안정”, 내용 미검사 marker count는 없다. 다음 JSON artifact를 CI/rollout job이 생성하고 모두 true일 때만 B6의 `Composite`를 켤 수 있다.

| 필드 | 고정 판정식 | 4987 근거 |
|---|---|---|
| `observation_days_complete` | B2+B3가 같은 revision으로 배포된 뒤 누락 없는 UTC day bucket **30개 이상** | B2 |
| `obligations_nonzero` | `total_obligations > 0`; 0이면 비율 게이트는 통과 불가 | 공허 통과 방지 |
| `dedup_ratio_ok` | `suppressed_by_dedup / total_obligations < 0.001` | B2, §7.4 |
| `tick_budget_ok` | 관측 구간 `tick_duration_p99_ms < 30_000` **그리고** `tick_overrun_count == 0` | B3/A8의 30s watchdog budget. 넘으면 shard/주기 재설계 |
| `schema_mutations_ok` | canonical schema mutation 전부 killed, survived=0 | B1′ |
| `registry_path_observed` | 사고 재현 또는 지정 fixture artifact가 `registry_output_path`와 선택 결과를 1건 이상 기록. native 여부는 true/false 모두 허용하되 문서 주장 강도를 그 값으로 갱신 | B4. 안전성을 특정 값에 의존시키지 않음 |
| `i14_lint_ok` | `scripts/check_reachability_row_independence.py`가 `ci-script-checks.sh`에서 실행되어 rc=0이고, 금지 `inflight::` import mutation이 실패하며, `relay_reachability` change-surface tracked output이 일치 | 4987 §-1.5 I14, §-1.6 신규 게이트, §9.4 **[r2-리뷰 P1-4 반영]** |
| `i15_choice_recorded` | 값이 정확히 `source_lint_convention`; private constructor/warrant를 만들지 않았음을 artifact에 기록 | B5 결정. 타입 보장은 T5 |

운영 cohort(내부 → 실채널 1 → 10% → 전체)는 배포 blast-radius 제어일 뿐 정확성 증명으로 세지 않는다. 각 단계는 같은 `G-T4` artifact를 재계산하지만 “사고가 없었다”를 gate 필드로 쓰지 않는다.

---

## 5. 롤백과 부분 착지 상태

### 5.1 스위치 semantics

| 스위치/조작 | 값 | 정확한 의미 |
|---|---|---|
| `ExecutionIdentityMode` | `Legacy` | nonce를 deny에 쓰지 않는다. 기존 `(session, output_path, cancel pointer)` registry CAS는 유지한다. **A1에서 제거한 #5067 emission fence 두 사이트는 복원하지 않는다.** |
|  | `Observe` (초기값) | Legacy 동작 + captured/current nonce와 owner 결과를 카운터/로그로 남긴다. |
|  | `Enforce` | T3-A1/A2의 자동 경로에서 positive owner/handle + `Some(nonce)` exact match가 아니면 cancel/remove를 거부한다. operator/process/미이관 경로에는 영향 없음. |
| `RelayVerdictSource` | `Structural` (초기값) | B0~B5를 shadow/health-detail로만 노출. 기존 구조 판정이 최종 건강을 결정한다. |
|  | `Composite` | 4987 `worst(ReachabilityVerdict, ExternalRelayVerdict)`를 최종 건강에 사용. 구조 신호는 행동 선택만 한다. |
| PR revert | A1/A2/A3/B6 | 삭제·호출부 이관의 유일한 코드 복원 수단. config switch가 삭제된 코드를 되살리지 않는다고 명시한다. |

### 5.2 부분 착지 상태 전수

| 상태 | 동작 | 롤백/위험 |
|---|---|---|
| A0만 | identity read/계측만. 파괴 변화 없음 | mode를 Legacy로; 코드 revert 불필요 |
| A1 착지, A2/A3 미착지 | H6 두 형제는 helper 비대칭에 맞춰 store-after-CAS. #5067 emission fence 두 사이트는 없음 | `Observe/Legacy`는 nonce deny만 끄며 두 fence는 돌아오지 않는다. baseline 복원은 **A1 PR revert**. same-incarnation emission race는 §8 비보장 **[r2-리뷰 P1-1 반영] [r2-리뷰 P1-2 반영]** |
| A2만 추가 | stale post-stream exit가 새 registry slot을 지우지 않음 | 항상 강화; 문제 시 A2 revert |
| A3만 추가 | no-transition가 applied로 계상되지 않음 | identity switch와 무관; A3 revert |
| B0/B1 | 내부 read library만. 판정 변화 없음 | PR revert 가능 |
| B2/B3/B4/B5, `Structural` | 의무/receipt/external verdict를 관측하나 최종 건강·복구 결정은 기존 동작 | 해당 slice revert 또는 sidecar reader 무시. sidecar 파일은 남아도 권위 없음 |
| B6 코드 착지, 여전히 `Structural` | Composite 코드가 dormant | switch 유지 |
| B6 `Composite` | non-Reachable은 건강을 선언하지 못함. reachability 단독 destructive action 없음 | 즉시 `Structural` 복귀. 계약/sidecar는 남음 |
| B6 후 stall-shadow 소비 −40 제거 | switch를 Structural로 돌려도 삭제 코드는 자동 복원되지 않음 | B6 PR revert가 유일한 완전 복원 |
| S5/S7 미착지 | 감사 probe/자동 재배달 없음 | 의도된 T5/NO-GO 상태 |

**롤백 한계:** A1의 #5067 fence 두 사이트 삭제 뒤 `Legacy`는 측정 SHA와 byte-equivalent가 아니다. 이 사실 때문에 “nonce CAS가 fence의 상위호환”이라고 주장하지 않는다. 장애 시 A1 전체 revert가 명시된 복원 절차다. **[r2-리뷰 P1-2 반영]**

---

## 6. 예산 — 삭제 크레딧 없이 산정

### 6.1 4987 숫자의 직접 검산

4987 §9.2 표의 prod 예상치를 그대로 더하면 다음과 같다.

| 4987 슬라이스 | Rust addition | 별도 |
|---|---:|---:|
| S0 | **정본 추정치 없음** | `+40~60`으로 가격하지 않음 **[r2-리뷰 P2-3 반영]** |
| S1 | +~940 | |
| S2 | +~240 | |
| S3 | +~210 | deletion −~40 |
| S4 | +~260 | |
| S5 | +~280 | |
| S6 | +~180 | Python +~120 |
| **§9.2 합(S1~S6)** | **Rust gross +2,110** | **Rust −40, Python +120** |

R2 §-1.6가 추가한 S0에는 라인 추정치가 없다. 따라서 `+40~60`을 정본 수치로 더하지 않는다. 현재 직접 검산 가능한 계획값은:

```
Rust gross additions  +2,110  + unpriced S0
Rust deletions           −40
Python additions         +120
확정 subtotal net      +2,190  + unpriced S0  (S0 제외만으로도 약 +2.2k 순증)
```

이것은 4987 §9.2의 숫자를 산술 검산하고 §-1.6 S0를 미가격으로 남긴 결과다. 순증이라는 결론은 S0를 0으로 놓아도 불변이다. 왜 그 숫자가 #5071에 “미계상”됐는지 귀책을 주장하지 않는다. **[r2-리뷰 P2-3 반영]**
또한 4987 §-1.5/§-1.6은 B1 범위가 1라운드 추정보다 커졌다고 명시한다. 따라서 위 값은 **현 구현의 실측 diff나 상한이 아니라 정본 표의 계획값**이다. 실제 예산 판정은 각 PR의 `git diff --numstat` 합을 누적해 대체해야 한다.

### 6.2 이 R2의 착지 범위

| 트랙 | 포함 | 정직한 예산 판정 |
|---|---|---|
| T4 본 시리즈 | S0,S1,S2,S3,S4,S6. S5는 T5 | §9.2 합에서 S5 +280을 빼면 **Rust gross +1,830, Rust deletion −40, Python +120**, 따라서 확정 subtotal은 **+1,910 + unpriced S0**다. 종전 `+1,950~+1,970`은 정본에 없는 S0 `+40~60`을 더한 값이므로 사용하지 않는다. **[r2-리뷰 P2-3 반영]** |
| T3 최소 identity/fence | A0~A4 | 구현 diff가 없으므로 prod 순증감을 숫자로 만들지 않는다. 각 PR은 additions/deletions 각각 800 이하이나 이는 예산 추정이 아니라 hard cap이다. |
| 확정 회수 | A1의 #5067 fence **두 사이트**(`health/relay_dead_reattach.rs:88` 선행 read/전달과 `apply.rs:216-224` 후행 read) 삭제; 4987 S3의 `stall_verdict` 경로 −~40 | A1 fence는 구현 diff 전 행수를 새로 가격하지 않는다. replacement addition과 S3 +210에 각각 포함되므로 별도 순삭제 크레딧으로 재사용하지 않는다. **[r2-리뷰 P1-2 반영]** |
| 후보 회수 | heartbeat, restored-owner, health/recovery, stall-liveness, actor/funnel 외 경로 | **0 크레딧**. 정독·이관 전 수치 없음. T5 표로만 보낸다. |

**판정:** 현재 정본 계획값과 이 R2의 삭제 확약만 놓으면 T3·T4의 방향은 **순증**이다. T4는 S0를 빼도 확정 subtotal이 +1.91k이고 T3에는 이를 상쇄할 확약 삭제가 없다. 다만 정확한 총량은 S0·T3 미가격과 B1 확대 때문에 주장하지 않는다. #5071의 T3 `−1.0~1.5k`, T4 `순 −2.5k` 목표를 이 계획이 충족한다고 쓰지 않는다. 결론은 종전과 불변이다. **[r2-리뷰 P2-3 반영]**

### 6.3 회수 귀속표

| 회수 항목 | 어느 슬라이스/트랙 | 이번 예산 반영 |
|---|---|---|
| #5067 임시 fence 두 사이트 | T3-A1, 대체 CAS와 같은 PR | `relay_dead_reattach.rs:88` 선행 read/전달과 `apply.rs:216-224` 후행 read를 함께 반영하되, 구현 diff 전 행수로 가격하거나 순삭제로 이중계상하지 않음 **[r2-리뷰 P1-2 반영]** |
| stall shadow 소비 약 40행 | T4-B6 (=4987 S3) | 4987 net에 1회 반영 |
| Tier C probe +280 | T5 | 삭제가 아니라 추가. T3T4 예산에서 제외 |
| 구조 health/heartbeat/restored-owner 철거 | T5 | 미가격, 크레딧 0 |
| legacy tmux kill/process/registry path 축소 | T5 | 미가격, 크레딧 0 |

---

## 7. 정본 정정 요구 — 입증된 사실만

### 7.1 #5071 본문

1. **§6 T3/T4 예산:** T3 `−1.0~1.5k`, T4 `순 −2.5k`, 전체 계는 이 구현 계획의 예산으로 사용할 수 없다. 4987 §9.2의 S1~S6 직접 합은 §6.1처럼 +2.19k net이며 S0는 미가격이다. 이 R2 T4 필수 범위도 확정 subtotal +1.91k와 미가격 S0다. T3는 diff 전 미가격이다. **[r2-리뷰 P2-3 반영]**
2. **§8 #5037/#5021:** `#5021` no-transition 회계는 T3-A3에서 닫을 수 있다. 반면 #5037의 cross-node emission과 local same-incarnation emission 경쟁은 이 최소 T3가 닫지 않는다. “T3까지 노출”을 “T3에서 완전 폐쇄”로 읽으면 안 되며 §8/T5 비보장을 붙여야 한다.
3. **4987 줄수:** 본문은 1,407줄이라 적지만 측정 SHA의 `wc -l docs/design/4987-relay-reachability.md`는 **1,406**이다.
4. **§8 nonce CAS 상위호환 문구:** 측정 SHA에서 #5067 fence는 `health/relay_dead_reattach.rs:88`과 `relay_recovery/apply.rs:216-224` 두 사이트다. A1은 둘을 함께 제거하지만 nonce/registry CAS는 same-incarnation emission exclusion을 제공하지 않으므로 “nonce CAS가 fence의 상위 호환”이라는 문구를 삭제하고 §8/T5 비보장을 유지해야 한다. **[r2-리뷰 P1-2 반영]**

### 7.2 4987 문서

1. **stale giant 수치:** 4987 §9.1의 `tmux_watcher.rs 3569줄 / 여유 2줄`은 현 SHA에서 **2,544줄**이다. 이 사실 정정은 S7의 범위 밖 판정을 자동으로 뒤집지 않는다.
2. **현재 파일 수치:** `tmux_watcher_registry.rs` 392줄, `relay_recovery/apply.rs` 402줄, `tmux_session_files.rs` 1,174줄, `relay_watchdog.py` 5,766줄이다. 새 PR의 giant/admission 판정은 현 파일을 다시 측정해야 한다.
3. **I15 강제 수준:** 4987 §-1.5가 이미 허용한 선택대로 이 R2는 private destructive constructor를 도입하지 않는다. 따라서 I15는 source lint + 계약 테스트 수준이며 타입 증명이 아니라는 문장을 B6 산출물에 유지해야 한다.
4. **S0 표적과 stale `mod.rs` 수치:** 4987 §-1.6의 `mod.rs 5,808줄`과 그 파일을 S0의 `TmuxWatcherBinding` 변경 표적으로 둔 서술은 현 코드와 맞지 않는다. 측정 SHA에서 `src/services/discord/mod.rs`는 **4,097줄**이고 해당 위치는 재export뿐이다. 실제 표적은 `tmux_watcher_registry.rs`(392줄)와 `health/session_enrichment.rs`(296줄)이므로 B0에는 giant admission note가 없다. `health/snapshot.rs`(1,279줄)를 실제로 건드릴 때만 그 파일로 admission 대상을 재지정한다. **[r2-리뷰 P2-2 반영]**
5. **I14/change-surface 집행 누락 금지:** 4987 §-1.5/§-1.6의 `scripts/check_reachability_row_independence.py` + `ci-script-checks.sh` 배선과 §9.4의 `docs/agent-maintenance/change-surfaces.md` 신규 `relay_reachability` surface를 구현 정정 요구로 유지한다. 이 FINAL은 둘 다 T4-B1 전용 게이트 및 G-T4의 `i14_lint_ok`에 배정한다. **[r2-리뷰 P1-4 반영]**

### 7.3 migration 적용선

2026-08-16 이 세션에서 production tunnel에 read-only 질의를 재실행했다.

```sql
SELECT max(version), string_agg(version::text, ',' ORDER BY version)
FROM _sqlx_migrations WHERE success;
-- 109 | 100,101,102,103,104,105,106,107,108,109

SELECT count(*) FROM delivery_journal_events;
-- 0
```

따라서 이전 통합 설계의 “0104 이하/0104 유지” 서술은 사실 오류다. 적용선은 **0109**, 현재 `delivery_journal_events`는 **0행**이다. 이 사실은 async journal을 cross-node barrier로 승격시키지 않는다.

---

## 8. 비보장 선언과 T5 이관

| 비보장 | 실제 사고 시나리오 | T5 이관 산출물 |
|---|---|---|
| **cross-node emission/정온** | A의 bounded journal event가 영구 drop된 사이 B가 quiet로 보고 cleanup/복구 | cluster-wide synchronous admission 또는 명시적 Unknown authority 설계. 0109 적용 사실과 별개 |
| **same-incarnation emission exclusion** | A1에서 #5067 임시 fence 두 사이트가 제거된 뒤 동일 watcher의 terminal POST 중 destructive cancel이 registry/nonce CAS를 통과 | 기존 delivery lease의 소유권 전달을 먼저 정식 설계하거나, fence 제거 계약 자체 재검토. 이 R2는 새 holder/actor를 만들지 않음 **[r2-리뷰 P1-2 반영]** |
| **registry row ABA 재입장** | pin된 5-튜플과 동일한 owner/output/session·동일 cancel Arc·미변경 nonce로 원상복귀 재삽입된 row는 값 기반 CAS가 교체 이력을 식별 불가 — Enforce에서도 통과. 프로덕션 writer 전수(a8e546b07 측정: `insert_locked` 단일 진입, 구성 사이트 7곳 전부 fresh Arc 생성)상 이 시퀀스를 구성하는 실경로 없음, 적대 구성만 가능. 문서화 테스트 `value_cas_declared_non_guarantee_readmitted_identical_row_passes_enforce`가 고정 **[A1-r2 legS P1 정오, 2026-08-16]** | row 세대/incarnation counter가 필요해지면 T5에서 registry 구조와 함께 설계. 값 CAS에 세대 의미를 소급 부여하는 주석 금지 |
| **spawn rename과 kill의 선형 CAS** | nonce 재확인 직후 같은 이름의 새 세션이 spawn되고 name-only kill이 새 세션을 죽임 | session creation과 destructive syscall이 공유하는 실제 atomic authority; 단순 파일 read 재시도는 해법으로 인정하지 않음 |
| **process backend identity** | stale PID 또는 재사용 PID에 `kill_pid_tree`가 적용되어 다른 실행을 종료 | PID start-time/pidfd 등 process-native identity 설계. tmux nonce 재사용 금지 |
| **ownerless 자동 cleanup 전역** | restore 지연을 owner absence로 오판해 정상 session/watcher 제거 | positive ownership이 없는 자동 파괴를 없애거나 별도 durable ownership authority 설계 |
| **registry 직접 remove 전역** | await 뒤 늦은 caller가 같은 channel의 새 watcher slot 제거 | 17-site 의미 판독 후 existing-handle CAS로 배치 이관; A2는 명백한 stale-exit 두 경로만 닫음 |
| **I15 타입 봉인** | 미래 코드가 `ReachabilityVerdict::Unreachable`만 보고 destructive action을 추가하고 source lint를 우회 | crate/type boundary 또는 sealed constructor. 이번에는 convention임을 공개 |
| **host-local sidecar의 cluster authority** | 다른 노드가 local obligation/external verdict를 못 보고 GREEN 유지 | PG/cluster authority migration과 immutable checksum 등록을 포함한 별도 설계 |
| **Tier C receipt audit** | local receipt가 있지만 Discord 실물은 사라진 거짓 양성을 B6가 직접 확인 못함 | 4987 S5를 T5로 구현. 실패는 판정 악화가 아니라 별도 auditor alarm |
| **자동 range 재배달** | 미커버 range를 검출해도 사용자가 직접 복구해야 하고 수동 개입이 중복을 만들 수 있음 | 4987 S7은 계속 NO-GO. 별도 설계와 dedup 계약 필요 |
| **구조 신호/권위 코드 삭제** | 새 판정기가 살아도 옛 heartbeat/restored-owner/health 분기가 중복 판단을 계속함 | T5에서 소비자 전수 정독 뒤 철거. 이 R2는 삭제량을 선반영하지 않음 |

T5는 위 항목을 한 번에 닫는 “새 거대 기계”가 아니다. 각 항목은 독립 이슈와 독립 반례를 가져야 하며, 해결하지 못하면 비보장으로 계속 남는다.

---

## 9. 미검증 선언

1. 저장소는 읽기 전용으로 다뤘다. `cargo check`, `cargo test`, formatter, mutation runner, inventory generator를 실행하지 않았다.
2. 모든 슬라이스의 “단독 컴파일 가능”은 파일 의존성 판독에 근거한 설계 주장이다. 구현 PR에서 §4.1을 통과하기 전에는 확인 사실이 아니다.
3. T3-A1에서 `SessionIncarnationRef`를 어느 구조체에 실어 나르는 것이 최소 diff인지 구현하지 않았다. 새 field가 `TmuxWatcherHandle` 리터럴 28파일을 건드리면 그 안은 기각하고 callsite-local capture 또는 더 작은 carrier를 택해야 한다.
4. A1의 nonce read와 marker rename 사이에는 공통 lock이 없다. 이 때문에 linearizable CAS를 보장하지 않으며 §8로 이관했다.
5. process kill 5 prod site의 PID provenance와 PID reuse window는 정독하지 않았다. 좌표와 호출 성격만 판독했다.
6. registry 제거 17곳 중 A1/A2 밖 13곳의 await/lifetime 시퀀스는 전량 정독하지 않았다. 전역 안전을 주장하지 않는다.
7. 4987의 transcript discovery 비용, 1MB cap, 30초 tick p99는 측정하지 않았다. `p99 < 30s`는 구현 후 판정할 hard gate다.
8. 30일 observation 결과와 `SuppressedByDedup` 분모는 아직 없다. 0-obligation cohort는 gate를 통과하지 못한다.
9. Rust↔Python canonical fixture 코퍼스와 mutation은 아직 존재하지 않는다. B2 전에는 S1 관측을 활성화하지 않는다.
10. `registry_output_path`가 과거 #4986 사고에서 실제 native transcript였는지는 원본 덤프가 없어 미검증이다. B4 측정값이 false여도 non-GREEN 설계는 유지하되 divergence 주장 강도를 낮춘다.
11. 4987 S3가 기존 `plan_relay_recovery`의 모든 action 조합에서 reachability-only destruction을 만들지 않는지는 구현 전 테이블 전수 테스트가 필요하다.
12. T3 prod 순증감은 미가격이다. 이전 라운드의 추정치를 새 최소안에 재사용하지 않았다.
13. production DB 질의는 read-only로 적용선과 journal row count만 확인했다. 다른 노드/다른 DB의 적용 상태는 확인하지 않았다.
14. 이 문서는 #5037 완전 폐쇄, 모든 session-name consumer fence, 모든 registry mutation CAS, process kill 안전, cross-node authority, 자동 복구 성공을 보장하지 않는다.

---

## 10. 최종 실행 판정

| 슬라이스 | 구현 진입 판정 | 근거/조건 |
|---|---|---|
| **T3-A3** | **진행 중** | 즉시 가능 판정 뒤 이미 구현 디스패치됨. P1 네 건과 무관 |
| **T3-A0** | **진행 중** | 즉시 가능 판정 뒤 이미 구현 디스패치됨. 파괴 동작 변화 없음 |
| **T3-A2** | **GO** | Drop guard와 동일 helper 호출의 중복임을 명시함(P2-4) |
| **T3-A4** | **GO** | registry baseline 오분류를 10/2/3/2로 정정함(P2-1) |
| **T3-A1** | **GO — P1-1/P1-2 반영 후 진입** | helper별 cancel 비대칭과 성공 후 store를 명시하고, `relay_dead_reattach.rs`를 포함한 #5067 fence 두 사이트 동시 처리를 확정함 **[r2-리뷰 P1-1 반영] [r2-리뷰 P1-2 반영]** |
| **T4-B0** | **GO** | admission note 대상 없음/조건부 `health/snapshot.rs` 대상으로 재지정함(P2-2) |
| **T4-B1** | **GO — P1-3/P1-4 반영 후 진입** | verdict 집합에 `TransportUnknown`을 포함하고 I14 린트/change-surface 집행기를 배정함 **[r2-리뷰 P1-3 반영] [r2-리뷰 P1-4 반영]** |
| **T4-B2** | **조건부 GO** | B1 착지 뒤. B1의 정정된 verdict 타입과 fixture schema를 소비 |
| **T4-B3/B4/B5** | **조건부 GO** | B1 정정 뒤 각 §4.3 순서 계약을 따름 |
| **T4-B6 코드** | **조건부 GO** | B2+B3+B4+B5 뒤이며, 4987 §-1.4의 7개 반례와 I14 게이트를 통과해야 함 |
| **T4-B6 `Composite` 활성화** | **NO-GO 유지** | `G-T4` 전 항목 통과 전 활성화 금지 |

**GO:** 진행 중인 T3-A3/A0을 계속하고, T3-A2/A4/A1 및 T4-B0~B5는 위 슬라이스별 조건과 기계 게이트 아래 구현 가능하다. T4-B6 코드는 선행 슬라이스 뒤 조건부 착지할 수 있다.

**NO-GO:** 새 DestructionActor/Warrant/ownerless proof/adoption/authority lock/common destroyer lease, async journal을 이용한 cross-node quiet proof, 4987 S7 자동 재배달, 삭제량을 맞추기 위한 구조 코드 선삭제.

**예산 판정:** 순증. 삭제 목표 미달을 숨기지 않고 T5에 실현되지 않은 삭제 크레딧을 넘기지 않는다.

T3T4-FINAL: DONE
